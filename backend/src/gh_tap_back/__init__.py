"""
# Gloomhaven TAP Backend

TBD

- [Docs](https://civanpima.github.io/gh_tap_back/gh_tap_back.html).

## Installation

```shell
pip install gh_tap_back
```

## Usage

...TBD

## Contributing

Once cloned the repo, install the pre-commit hooks:

```shell
pre-commit install --install-hooks
```

Install the library (in a virtual environment) as an editable package with the development dependencies:

```shell
pip install -e ".[dev]"
```

### Documentation

- This library uses the [numpydoc](https://numpydoc.readthedocs.io/en/latest/format.html) format for the docstrings.
- To generate the documentation, run:

```shell
pdoc src/gh_tap_back --mermaid --docformat numpy
```

### Testing

To run the tests:
```shell
behave
```
"""

__version__ = "0.0.0"
